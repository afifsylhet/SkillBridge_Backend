{
  "buildCommand": "echo 'no static build — Vercel bundles api/ via @vercel/node'",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/health", "destination": "/api" }
  ]
}
